# allow for tab autocompletion with sudo and man
complete -cf sudo
complete -cf man

# travis ci
[ -f ~/.travis/travis.sh ] && source ~/.travis/travis.sh

# git flow
[ -f ~/.bash/git-flow-completion.sh ] && source ~/.bash/git-flow-completion.sh
